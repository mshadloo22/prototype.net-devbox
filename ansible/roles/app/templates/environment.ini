{{ config_prefix }}_database_host = "{{database_host}}"
{{ config_prefix }}_database_port = "{{database_port}}"
{{ config_prefix }}_database_user = "{{database_user}}"
{{ config_prefix }}_database_password = "{{database_password}}"
{{ config_prefix }}_database_name = "{{database_name}}"

foo = "{{foo}}"